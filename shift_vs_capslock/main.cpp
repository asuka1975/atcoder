#include <iostream>
#include <string>
#include <vector>

/*
dp[i + 1] = 

*/

int main() {
    int x, y, z;
    std::string s;

    std::cin >> x >> y >> z;
    std::cin >> s;

    std::vector<char> dp_op(600000);
    std::vector<int>  dp_sc(600000);


}